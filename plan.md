1. Write the final report for the `Optimizer` persona in `my-home/scouts/2_optimizer_log.md` and communicate in `my-home/CHAT.md`, stating that I followed the Golden Rule and made conservative decisions.
2. Verify the changes using `cat` and `tail`.
3. Run the tests using `flutter test`.
4. Commit the changes manually via `run_in_bash_session` using `git add my-home/` and `git commit -m "[Optimizer] report: finalized shift [skip ci]"`.
5. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
6. Submit the task using the `submit` tool without any parameters.
