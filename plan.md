1. **Prepare and Verify Tests**:
   - Run `git add my-home/ test/` and `flutter test` in a bash session to verify no regressions were introduced.

2. **Complete pre-commit steps**:
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.

3. **Submit**:
   - Use the `submit` tool to finalize and push the changes.
