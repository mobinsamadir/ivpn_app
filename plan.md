1. **بررسی بیلد ترکیبی Android + Windows:**
   - فایل مورد نظر `.github/workflows/main_pipeline.yml` است.
   - وضعیت کنونی: هر دو جاب `build-windows` و `build-android` در حال حاضر موازی اجرا می‌شوند، زیرا هر دو فقط به جاب `changes` وابستگی (`needs: changes`) دارند. پس به صورت پیش‌فرض (توسط گیت‌هاب اکشنز) این دو جاب سری نیستند و به طور همزمان اجرا می‌شوند تا زمان کلی کاهش یابد.

2. **بررسی تأثیر `flutter build appbundle --debug` بر زمان کل:**
   - دستور `flutter build appbundle --debug` در فایل `.github/workflows/pre_pr_checks.yml` (در جاب `android-build`) قرار دارد و جزو فرآیند pre-PR است.
   - این دستور و کلاً ورک‌فلوی `pre_pr_checks.yml` کاملاً از `main_pipeline.yml` جداست. یعنی اجرای آن تاثیری بر زمان کلِ بیلد `main_pipeline.yml` ندارد.
   - البته با توجه به اینکه در صورت تایید PR و مرج شدن به main هر دو ورک‌فلو ممکن است اجرا شوند (بسته به تنظیمات تریگرها)، در صورت اجرای دستی `main_pipeline.yml`، تکراری نخواهد بود. اما در هر حال این دو در ورک‌فلوهای جداگانه‌ای هستند و با هم ادغام نشده‌اند. اگر هدف کاهش زمان Pre-PR Check است، می‌توان حذف آن را بررسی کرد.

3. **گزارش نسخه‌های فعلی Actions در ورک‌فلوها و آخرین نسخه‌های موجود:**
   بر اساس جستجو در تمامی فایل‌های `.github/workflows/`:

   - **`actions/cache`**: نسخه فعلی `@v4` | آخرین نسخه: `@v6`
   - **`actions/checkout`**: نسخه فعلی `@v4` و `@v5` | آخرین نسخه: `@v7`
   - **`actions/setup-go`**: نسخه فعلی `@v5` | آخرین نسخه: `@v6`
   - **`actions/setup-java`**: نسخه فعلی `@v4` | آخرین نسخه: `@v5`
   - **`actions/setup-python`**: نسخه فعلی `@v5` | آخرین نسخه: `@v6`
   - **`dorny/paths-filter`**: نسخه فعلی `@v3` | آخرین نسخه: `@v4`
   - **`gradle/actions/setup-gradle`**: نسخه فعلی `@v4` | آخرین نسخه: `@v6`
   - **`nttld/setup-ndk`**: نسخه فعلی `@v1` | آخرین نسخه: `@v1.6.0` (در اینجا تگ `v1` به آخرین پچ نسخه 1 اشاره می‌کند)
   - **`softprops/action-gh-release`**: نسخه فعلی `@v1` | آخرین نسخه: `@v3`
   - **`subosito/flutter-action`**: نسخه فعلی `@v2` | آخرین نسخه: `@v2.23.0` (تگ `v2` همچنان معتبر است)

4. **Complete pre commit steps:**
   - هیچ فایلی تغییر نکرده است.

5. **ارسال گزارش:**
   - ارسال گزارش نهایی به کاربر.
